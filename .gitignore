node_modules
node_modules
.env.local
.next

# clerk configuration (can include secrets)
/.clerk/

# Pre-launch data files (contain emails and access tokens)
data/subscribers.json
data/access-tokens.json
.vercel
