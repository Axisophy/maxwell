# MXWLL Preview Access Configuration
# Copy this to .env.local and fill in the values

# The password reviewers will use to access the dev site
# Choose something memorable but not guessable
PREVIEW_PASSWORD=your-preview-password-here

# A random token for the auth cookie (generate with: openssl rand -hex 32)
# This should be a long random string
PREVIEW_AUTH_TOKEN=generate-a-random-token-here

# Admin key for generating access links (generate with: openssl rand -hex 16)
# Keep this secret - it lets you create access links
ADMIN_KEY=generate-another-random-key-here

# Your site URL (used for generating full access links)
NEXT_PUBLIC_SITE_URL=https://mxwll.io

# Example of how to generate tokens:
# Run this in terminal: openssl rand -hex 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
